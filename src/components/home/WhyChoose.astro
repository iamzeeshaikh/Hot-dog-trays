<section class="hdt-why-choose">
    <div class="hdt-why-choose-container">
        <div class="hdt-why-choose-header">
            <div class="hdt-why-choose-badge">Our Advantages</div>
            <h2 class="hdt-why-choose-title">Why Choose HotDogTrays.com?</h2>
            <p class="hdt-why-choose-subtitle">
                Industry-leading quality, service, and pricing. Here's what makes us the #1 choice for hot dog trays.
            </p>
        </div>
        <div class="hdt-benefits-grid">
            <div class="hdt-benefit-card">
                <div class="hdt-benefit-icon">🎨</div>
                <h3 class="hdt-benefit-title">Free Customization</h3>
                <p class="hdt-benefit-description">
                    Get your brand noticed with custom-printed hot dog trays at no extra design cost.
                </p>
                <div class="hdt-benefit-features">
                    <span class="hdt-benefit-feature">Free design consultation</span>
                    <span class="hdt-benefit-feature">Full-color printing available</span>
                    <span class="hdt-benefit-feature">Logo & branding included</span>
                </div>
            </div>
            <div class="hdt-benefit-card">
                <div class="hdt-benefit-icon">⚡</div>
                <h3 class="hdt-benefit-title">Fast Turnaround</h3>
                <p class="hdt-benefit-description">
                    Get your order quickly with our expedited production and shipping services.
                </p>
                <div class="hdt-benefit-features">
                    <span class="hdt-benefit-feature">7-10 day standard production</span>
                    <span class="hdt-benefit-feature">Rush options available</span>
                    <span class="hdt-benefit-feature">Express shipping to all regions</span>
                </div>
            </div>
            <div class="hdt-benefit-card">
                <div class="hdt-benefit-icon">♻️</div>
                <h3 class="hdt-benefit-title">Eco-Friendly Options</h3>
                <p class="hdt-benefit-description">
                    Choose sustainable materials that are Recyclable paperboard options.
                </p>
                <div class="hdt-benefit-features">
                    <span class="hdt-benefit-feature">recyclable paperboard options</span>
                    <span class="hdt-benefit-feature">Biodegradable options</span>
                    <span class="hdt-benefit-feature">Responsibly sourced board</span>
                </div>
            </div>
            <div class="hdt-benefit-card">
                <div class="hdt-benefit-icon">💰</div>
                <h3 class="hdt-benefit-title">Wholesale Pricing</h3>
                <p class="hdt-benefit-description">
                    Save more with our competitive bulk pricing and volume discounts.
                </p>
                <div class="hdt-benefit-features">
                    <span class="hdt-benefit-feature">Starting at $0.50 USD</span>
                    <span class="hdt-benefit-feature">Volume discounts available</span>
                    <span class="hdt-benefit-feature">No hidden fees</span>
                </div>
            </div>
            <div class="hdt-benefit-card">
                <div class="hdt-benefit-icon">⭐</div>
                <h3 class="hdt-benefit-title">Quality Guarantee</h3>
                <p class="hdt-benefit-description">
                    Premium materials and strict quality control ensure perfect products every time.
                </p>
                <div class="hdt-benefit-features">
                    <span class="hdt-benefit-feature">food-grade materials</span>
                    <span class="hdt-benefit-feature">Grease-resistant coating</span>
                    <span class="hdt-benefit-feature">100% satisfaction guarantee</span>
                </div>
            </div>
            <div class="hdt-benefit-card">
                <div class="hdt-benefit-icon">💬</div>
                <h3 class="hdt-benefit-title">Expert Support</h3>
                <p class="hdt-benefit-description">
                    Our dedicated team is here to help you find the perfect solution for your needs.
                </p>
                <div class="hdt-benefit-features">
                    <span class="hdt-benefit-feature">24/7 customer support</span>
                    <span class="hdt-benefit-feature">Product recommendations</span>
                    <span class="hdt-benefit-feature">Order tracking & updates</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
/* Migrated from the Elementor "WhyChoose" HTML widget. */
/* Force everything clickable */
.hdt-why-choose,
.hdt-why-choose * {
    pointer-events: auto !important;
}

/* Why Choose Us Section */
.hdt-why-choose {
    background: linear-gradient(135deg, #1D3557 0%, #2A4A6F 100%);
    padding: 80px 0;
    position: relative;
    overflow: hidden;
}

/* Background Decoration */
.hdt-why-choose::before {
    content: '🌭';
    position: absolute;
    font-size: 250px;
    opacity: 0.03;
    top: 10%;
    right: -50px;
    animation: float 10s ease-in-out infinite;
}

@keyframes float {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-30px) rotate(15deg); }
}

.hdt-why-choose-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 1;
}

/* Section Header */
.hdt-why-choose-header {
    text-align: center;
    margin-bottom: 60px;
}

.hdt-why-choose-badge {
    display: inline-block;
    background: linear-gradient(135deg, #F9C74F, #E63946);
    color: #1D3557;
    padding: 8px 20px;
    border-radius: 50px;
    font-size: 0.85rem;
    font-weight: 800;
    margin-bottom: 20px;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.hdt-why-choose-title {
    font-size: 2.8rem;
    font-weight: 900;
    color: #F1FAEE;
    margin-bottom: 15px;
    line-height: 1.2;
}

.hdt-why-choose-subtitle {
    font-size: 1.1rem;
    color: rgba(241, 250, 238, 0.9);
    max-width: 650px;
    margin: 0 auto;
}

/* Benefits Grid */
.hdt-benefits-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
}

/* Benefit Card */
.hdt-benefit-card {
    background: rgba(241, 250, 238, 0.05);
    backdrop-filter: blur(10px);
    border: 2px solid rgba(241, 250, 238, 0.1);
    border-radius: 20px;
    padding: 35px 30px;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
}

.hdt-benefit-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: linear-gradient(90deg, #F9C74F, #E63946);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s ease;
}

.hdt-benefit-card:hover {
    background: rgba(241, 250, 238, 0.1);
    border-color: #F9C74F;
    transform: translateY(-8px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
}

.hdt-benefit-card:hover::before {
    transform: scaleX(1);
}

/* Benefit Icon */
.hdt-benefit-icon {
    width: 70px;
    height: 70px;
    background: linear-gradient(135deg, #F9C74F, #E63946);
    border-radius: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 25px;
    font-size: 2rem;
    box-shadow: 0 10px 25px rgba(249, 199, 79, 0.4);
    transition: all 0.3s ease;
}

.hdt-benefit-card:hover .hdt-benefit-icon {
    transform: scale(1.1) rotate(-5deg);
    box-shadow: 0 15px 35px rgba(230, 57, 70, 0.6);
}

/* Benefit Content */
.hdt-benefit-title {
    font-size: 1.4rem;
    font-weight: 800;
    color: #F1FAEE;
    margin-bottom: 15px;
    line-height: 1.3;
}

.hdt-benefit-description {
    font-size: 0.95rem;
    color: rgba(241, 250, 238, 0.8);
    line-height: 1.7;
    margin-bottom: 15px;
}

.hdt-benefit-features {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.hdt-benefit-feature {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 0.85rem;
    color: rgba(241, 250, 238, 0.9);
}

.hdt-benefit-feature::before {
    content: '✓';
    width: 22px;
    height: 22px;
    background: rgba(249, 199, 79, 0.2);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    color: #F9C74F;
    font-weight: 900;
    font-size: 0.9rem;
}

/* Responsive Design */
@media (max-width: 1024px) {
    .hdt-benefits-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 25px;
    }
}

@media (max-width: 768px) {
    .hdt-why-choose {
        padding: 60px 0;
    }
    
    .hdt-why-choose-title {
        font-size: 2.2rem;
    }
    
    .hdt-benefits-grid {
        grid-template-columns: 1fr;
        gap: 20px;
    }
    
    .hdt-benefit-card {
        padding: 30px 25px;
    }
}

@media (max-width: 480px) {
    .hdt-why-choose-title {
        font-size: 1.8rem;
    }
    
    .hdt-benefit-icon {
        width: 60px;
        height: 60px;
        font-size: 1.7rem;
    }
    
    .hdt-benefit-title {
        font-size: 1.2rem;
    }
}

/* Performance Optimization */
@media (prefers-reduced-motion: reduce) {
    .hdt-why-choose::before,
    .hdt-benefit-card,
    .hdt-benefit-card::before,
    .hdt-benefit-icon {
        animation: none !important;
        transition: none !important;
    }
}
</style>
